import React from 'react';
import CodeEditor from './CodeEditor';  // Adjust the import based on your folder structure

const App = () => {
    return (
        <div>
            <CodeEditor />
        </div>
    );
};

export default App;